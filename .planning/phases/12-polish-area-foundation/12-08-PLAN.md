---
phase: 12-polish-area-foundation
plan: 08
type: execute
wave: 4
depends_on: [12-05, 12-07]
files_modified: []
autonomous: false
requirements: [REQ-TAKE, REQ-BATCH, REQ-MULTI, REQ-SPLICE, REQ-VERIFY, REQ-UNDO, REQ-STAGE]

must_haves:
  truths:
    - "Complete take replacement workflow is functional end-to-end"
    - "Multi-waveform view loads and syncs correctly"
    - "Batch editor shows chapter selection and operation tabs"
    - "Staging queue operates non-destructively"
    - "Verification flow works after replacement"
---

<objective>
Verify the complete Polish area workflow end-to-end with human visual and functional verification.

Purpose: This is the final checkpoint to confirm all Polish area features work together correctly before marking the phase complete. The user tests the take replacement flow, multi-waveform view, batch editor, and verification loop.
Output: Verified working Polish area
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-area-foundation/12-05-SUMMARY.md
@.planning/phases/12-polish-area-foundation/12-06-SUMMARY.md
@.planning/phases/12-polish-area-foundation/12-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end Polish area verification</name>
  <files></files>
  <action>
Human verifies the complete Polish area workflow by testing all features in the running workstation. This checkpoint covers all 7 requirement areas (REQ-TAKE, REQ-BATCH, REQ-MULTI, REQ-SPLICE, REQ-VERIFY, REQ-UNDO, REQ-STAGE).

Steps:
1. Start workstation: `dotnet run --project "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj"`
2. Open a book with chapters that have CRX errors

Take Replacement Flow:
3. Navigate to /polish -- verify chapter list with CRX counts
4. Click into a chapter -- verify waveform, PickupImporter, StagingQueue visible
5. Import a pickup file -- verify ASR matching with confidence scores
6. Verify draggable regions on pickup waveform
7. Stage a match -- verify it appears in staging queue
8. Apply -- verify crossfade splice and ContextPlayer appears
9. Play with context -- verify surrounding audio plays
10. Accept fix -- verify Proof sync

Undo:
11. Revert an applied item -- verify original audio restored

Multi-Waveform and Batch:
12. Navigate to /polish/batch
13. Select chapters -- verify stacked waveforms with synced playheads
14. Verify rename, shift, pre/post roll tabs functional
15. Verify DSP tab shows placeholder

General:
16. Verify BitUI components render correctly
17. Verify no console errors
  </action>
  <verify>User confirms all features work by typing "approved" or describing issues.</verify>
  <done>All Polish area features verified as functional by human tester.</done>
</task>

</tasks>

<verification>
User confirms all Polish area features are functional via manual testing.
</verification>

<success_criteria>
- Take replacement workflow works end-to-end (import -> match -> adjust -> stage -> apply -> verify)
- Multi-waveform view syncs playheads
- Batch editor shows functional tabs
- Staging queue is non-destructive
- Undo restores original audio
- Verification auto-runs and syncs to Proof
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish-area-foundation/12-08-SUMMARY.md`
</output>
