---
phase: 15-pickup-flow-redesign
plan: 07
type: execute
wave: 5
depends_on: ["15-06"]
files_modified: []
autonomous: false
requirements: [PFR-MODELS, PFR-BREATH, PFR-IMPORT, PFR-MATCH, PFR-EDITLIST, PFR-REVERT, PFR-TIMELINE, PFR-DUAL, PFR-REASSIGN, PFR-CONTEXT]

must_haves:
  truths:
    - "Solution builds with 0 errors"
    - "Pickup import works for both session files and individual file folders"
    - "Pickups can be applied and reverted in any order without timeline corruption"
    - "Unmatched pickups can be manually assigned to CRX targets"
    - "Both chapter-side and pickup-side boundaries are adjustable"
    - "Context playback auditions pickup with surrounding chapter audio"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 15 redesign. Verify that the pickup substitution workflow works end-to-end with the new immutable edit list, rebuild-based revert, unified import, breath-aware boundaries, and dual-side boundary editing.

Purpose: Confirm the redesigned pickup flow works correctly before marking the phase complete.
Output: Verified Phase 15 functionality.
</objective>

<execution_context>
@/home/cari/.agents/skills/gsd/workflows/execute-plan.md
@/home/cari/.agents/skills/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-pickup-flow-redesign/15-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of Phase 15 pickup flow redesign</name>
  <files></files>
  <action>Human verification — see how-to-verify steps below.</action>
  <verify>All 15 verification steps pass and user approves.</verify>
  <done>User types "approved" confirming the redesigned pickup workflow works correctly.</done>
  <what-built>
Complete Phase 15 pickup flow redesign:
1. Immutable edit list with TimelineProjection (replaces ShiftDownstream)
2. Rebuild-from-original revert (arbitrary order revert)
3. Unified PickupAsset import (session files + individual files + folders)
4. Hybrid text-similarity matching for out-of-order session recordings
5. Breath-aware boundary placement via FeatureExtraction.Detect()
6. Dual-side boundary editing (chapter regions + pickup trim handles)
7. Unmatched bucket with manual assignment
8. Manual reassignment of any pickup to a different CRX target
9. Context playback for audition
  </what-built>
  <how-to-verify>
1. Start the workstation: `dotnet run --project host/Ams.Workstation.Server`
2. Navigate to /polish
3. Load a book with CRX entries

**Import test:**
4. Import a session file (single WAV with multiple pickups)
   - Verify pickups appear in Matches column with confidence scores
   - Verify any low-confidence matches appear in Unmatched bucket
5. Try importing a folder of individual files (e.g., 001.wav, 002.wav)
   - Verify files match to CRX entries by error number
   - Verify unmatched files appear in Unmatched bucket

**Matching test:**
6. If any pickups are unmatched, manually assign one to a CRX target
   - Verify it moves from Unmatched to Matches
7. Try reassigning a matched pickup to a different CRX target
   - Verify the reassignment takes effect

**Boundary editing test:**
8. Stage a pickup
   - Verify green region appears on chapter waveform at correct position
9. Drag chapter-side region handles — verify boundaries adjust
10. Click the PickupBox to open pickup detail panel
    - Verify pickup waveform loads with orange trim handles
    - Drag trim handles — verify trim boundaries adjust

**Apply/Revert test:**
11. Apply (commit) pickup A, then pickup B
12. Revert pickup A (applied first, reverted second — out-of-order)
    - Verify chapter audio is correct: pickup B still applied, A reverted
13. Revert pickup B
    - Verify chapter audio returns to original treated state

**Context playback test:**
14. Stage a pickup and click "Audition in Context"
    - Verify you hear surrounding chapter audio with the pickup spliced in

**Build verification:**
15. `dotnet build` succeeds with 0 errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all verification steps pass.
</verification>

<success_criteria>
- All 15 verification steps pass
- User approves the redesigned workflow
</success_criteria>

<output>
After completion, create `.planning/phases/15-pickup-flow-redesign/15-07-SUMMARY.md`
</output>
