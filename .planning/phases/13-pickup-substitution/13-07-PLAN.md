---
phase: 13-pickup-substitution
plan: 07
type: execute
wave: 4
depends_on: ["13-05", "13-06"]
files_modified:
  - host/Ams.Workstation.Server/Components/Pages/Polish/Index.razor
  - host/Ams.Workstation.Server/Components/Pages/Polish/ChapterPolish.razor
  - host/Ams.Workstation.Server/Components/Pages/Polish/PickupSubstitution.razor
  - host/Ams.Workstation.Server/Components/Shared/PickupImporter.razor
  - host/Ams.Workstation.Server/Components/Shared/StagingQueue.razor
autonomous: true
requirements: [PS-LAYOUT, PS-NAV, PS-COMPLETE]

must_haves:
  truths:
    - "No route conflict between old and new Polish pages"
    - "Progress indicator shows during initial pickup processing"
    - "Previous chapter audio deallocated on flipper navigation to prevent memory pressure"
    - "Solution builds cleanly with 0 errors"
  artifacts:
    - path: "host/Ams.Workstation.Server/Components/Pages/Polish/Index.razor"
      provides: "Redirects to /polish or is removed as a route"
    - path: "host/Ams.Workstation.Server/Components/Pages/Polish/PickupSubstitution.razor"
      provides: "Clean single-page workflow at /polish with no route conflicts"
      contains: "@page \"/polish\""
  key_links:
    - from: "host/Ams.Workstation.Server/Components/Pages/Polish/PickupSubstitution.razor"
      to: "host/Ams.Workstation.Server/Components/Pages/Polish/BatchEditor.razor"
      via: "Separate route /polish/batch preserved for batch operations"
      pattern: "/polish/batch"
---

<objective>
Clean up old Polish pages, resolve route conflicts, add progress indicators, and ensure memory-safe flipper navigation. This is the integration and cleanup pass.

Purpose: The Phase 12 Polish pages (Index.razor chapter list, ChapterPolish.razor per-chapter view) must be replaced by the new PickupSubstitution.razor. Route conflicts (Research Pitfall 7) must be resolved. Progress indicators and memory cleanup round out the experience.

Output: Clean routing, old page cleanup, progress UI, memory-safe navigation, build verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pickup-substitution/13-RESEARCH.md
@.planning/phases/13-pickup-substitution/13-05-SUMMARY.md
@.planning/phases/13-pickup-substitution/13-06-SUMMARY.md
@host/Ams.Workstation.Server/Components/Pages/Polish/Index.razor
@host/Ams.Workstation.Server/Components/Pages/Polish/ChapterPolish.razor
@host/Ams.Workstation.Server/Components/Shared/PickupImporter.razor
@host/Ams.Workstation.Server/Components/Shared/StagingQueue.razor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route cleanup and old page decommission</name>
  <files>
    host/Ams.Workstation.Server/Components/Pages/Polish/Index.razor
    host/Ams.Workstation.Server/Components/Pages/Polish/ChapterPolish.razor
    host/Ams.Workstation.Server/Components/Shared/PickupImporter.razor
    host/Ams.Workstation.Server/Components/Shared/StagingQueue.razor
  </files>
  <action>
    **1. Remove route from Index.razor** (Research Pitfall 7):
    The new PickupSubstitution.razor owns `@page "/polish"`. Index.razor must not conflict.
    - Option A (preferred): Remove the `@page "/polish"` directive from Index.razor entirely. Keep the file but make it a routeless "old reference" (or delete it if no other components reference it).
    - Option B: Delete Index.razor entirely since it's replaced by PickupSubstitution.razor.
    - **Choose Option B** — the locked decision says "This replaces the Phase 12 Polish page scaffold (Index.razor, ChapterPolish.razor, PickupImporter.razor, StagingQueue.razor)". Delete Index.razor.

    **2. Remove route from ChapterPolish.razor:**
    - Remove `@page "/polish/{ChapterName}"` from ChapterPolish.razor
    - The locked decision says ChapterPolish.razor is replaced. Delete ChapterPolish.razor entirely.

    **3. Mark PickupImporter.razor and StagingQueue.razor as deprecated (do NOT delete yet):**
    - These components may still be referenced by BatchEditor.razor or other pages
    - Check: `grep -r "PickupImporter\|StagingQueue" host/Ams.Workstation.Server/Components/`
    - If only referenced by now-deleted pages (Index, ChapterPolish): delete them too
    - If referenced by BatchEditor.razor or other surviving pages: keep them but add a comment `@* DEPRECATED: replaced by PickupBox.razor in PickupSubstitution.razor *@`

    **4. Verify BatchEditor.razor route is untouched:**
    - Confirm `/polish/batch` route still works on BatchEditor.razor
    - No conflicts since PickupSubstitution is at `/polish` (exact match) and BatchEditor is at `/polish/batch`

    **5. Update navigation links:**
    - Check if any navigation links point to `/polish/{chapterName}` (the old per-chapter route)
    - If found, update to use the new single-page approach (the PickupSubstitution page handles chapter selection internally via flippers)
    - Check the main layout nav: if there's a "Polish" nav link, it should point to `/polish`
  </action>
  <verify>
    Build: `dotnet build "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj" -c Debug`
    Verify: 0 errors. No ambiguous route warnings. `/polish` resolves to PickupSubstitution.razor.
  </verify>
  <done>
    Old Polish pages deleted (Index.razor, ChapterPolish.razor). No route conflicts. PickupImporter.razor and StagingQueue.razor cleaned up (deleted if unreferenced, deprecated if still used). BatchEditor.razor at /polish/batch preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Progress indicators, memory cleanup, and final build verification</name>
  <files>
    host/Ams.Workstation.Server/Components/Pages/Polish/PickupSubstitution.razor
  </files>
  <action>
    **1. Progress Indicator during Pickup Processing:**
    Verify the linear BitProgress bar in the header area shows during pickup processing (this was scaffolded in plan 03, wired in plan 04). Ensure:
    - BitProgress is linear (not circular): `<BitProgress Value="@(_processingProgress * 100)" />`
    - Status text updates through stages: "Splitting by silence...", "Running ASR...", "Running MFA alignment...", "Matching to CRX entries..."
    - Progress bar disappears when processing completes

    If the progress reporting from ImportPickupsCrossChapterAsync doesn't provide enough granularity, add intermediate status updates in the UI handler.

    **2. Memory Cleanup on Chapter Flip:**
    Ensure the NavigateToChapter method deallocates the previous chapter's audio buffer. The pattern from Research Pitfall 4:
    ```csharp
    private async Task NavigateToChapter(int index)
    {
        if (index < 0 || index >= _chaptersWithCrx.Count) return;

        // Deallocate previous chapter's corrected audio buffer
        if (_currentChapterIndex >= 0 && _waveformPlayer != null)
        {
            try
            {
                var prevChapter = _chaptersWithCrx[_currentChapterIndex];
                var handle = Workspace.GetChapterHandle(prevChapter.ChapterName);
                handle?.Chapter.Audio.Deallocate("corrected");
            }
            catch { /* Ignore deallocation errors */ }
        }

        _currentChapterIndex = index;
        var chapter = _chaptersWithCrx[index];
        Workspace.SelectChapter(chapter.ChapterName);

        // Reload chapter state
        await LoadChapterDataAsync(chapter);
        await SyncWaveformRegions();
        StateHasChanged();
    }
    ```

    **3. IAsyncDisposable Cleanup:**
    Ensure DisposeAsync properly cleans up:
    - Clear _matchesByChapter
    - Clear waveform player reference
    - Cancel any in-progress processing (add a CancellationTokenSource)

    **4. Edge Cases:**
    - No CRX entries at all: show "No chapters with CRX entries found" message instead of empty layout
    - No book loaded: show redirect to home page (same pattern as old Index.razor)
    - Pickup file path invalid: show error message, don't crash
    - Roomtone file path invalid: show error message when operation attempted

    **5. Full Solution Build Verification:**
    After all changes, build the full solution in Release mode and verify 0 errors.
  </action>
  <verify>
    Build: `dotnet build "C:\Projects\AMS\host\Ams.sln" -c Release`
    Verify: 0 errors, 0 new warnings. All routes resolve correctly.
  </verify>
  <done>
    Progress bar shows during pickup processing with stage-by-stage status text. Memory deallocated on chapter flip. IAsyncDisposable handles cleanup. Edge cases covered (no CRX, no book, invalid paths). Full solution builds cleanly in Release mode with 0 errors.
  </done>
</task>

</tasks>

<verification>
- `dotnet build "C:\Projects\AMS\host\Ams.sln" -c Release` passes with 0 errors
- No route conflicts in the Polish area
- Old Index.razor and ChapterPolish.razor deleted
- Progress indicators show during pickup processing
- Memory doesn't grow unbounded when flipping through chapters
- Edge cases produce user-friendly messages instead of crashes
</verification>

<success_criteria>
- Clean routing: /polish -> PickupSubstitution, /polish/batch -> BatchEditor
- No references to deleted pages remain in the codebase
- Progress UI visible during ASR/MFA processing
- Stable memory usage during multi-chapter navigation
- Full solution builds with 0 errors in Release mode
</success_criteria>

<output>
After completion, create `.planning/phases/13-pickup-substitution/13-07-SUMMARY.md`
</output>
