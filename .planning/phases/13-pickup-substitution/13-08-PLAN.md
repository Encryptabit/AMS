---
phase: 13-pickup-substitution
plan: 08
type: execute
wave: 5
depends_on: ["13-07"]
files_modified: []
autonomous: false
requirements: [PS-LAYOUT, PS-IMPORT, PS-MATCH, PS-PIPELINE, PS-REGIONS, PS-COMMIT, PS-ROOMTONE, PS-FORMAT, PS-NAV, PS-COMPLETE]

must_haves:
  truths:
    - "Full pickup substitution workflow is functional end-to-end"
    - "User can set pickup file and all chapters are processed upfront"
    - "Pickup boxes move through Match -> Stage -> Commit pipeline"
    - "Committed replacements produce format-matching corrected.wav"
    - "Roomtone operations work on arbitrary waveform regions"
    - "Flippers navigate between CRX chapters with auto-advance on completion"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for the complete pickup substitution workflow.

Purpose: Verify that all Phase 13 components work together as a cohesive workflow — from setting the pickup file through processing, matching, staging, boundary editing, committing, roomtone operations, and chapter completion with auto-advance.

Output: Human verification of the complete workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pickup-substitution/13-RESEARCH.md
@.planning/phases/13-pickup-substitution/13-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end pickup substitution verification</name>
  <files></files>
  <action>
    Human verification of the complete pickup substitution workflow built across plans 13-01 through 13-07.

    Start the workstation: `dotnet run --project "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj"`

    Verify the following:
    1. Navigate to /polish — single-page layout with header bar (pickup file + roomtone file selectors), breadcrumb area, waveform with flippers, three empty columns
    2. Set a pickup session WAV file path — progress bar shows with status text, processing completes, matches populate the Matches column
    3. Mini waveform thumbnails render in each match box (small canvas bars, not full wavesurfer)
    4. Breadcrumb shows current chapter position (e.g., "Chapter 1 (1/5) — 0/3 committed")
    5. Click "Stage" on a match box — box moves from Matches to Staged column, green region on waveform
    6. Drag a match box from Matches to Staged — drag-and-drop staging works
    7. Drag green region boundaries on the waveform — region resizes, boundaries update
    8. Click "Unstage" — box returns to Matches column, region removed from waveform
    9. Stage a match, click "Commit" — waveform reloads with updated audio, box in Committed column
    10. Click "Revert" on committed item — original audio restored, box returns to Matches
    11. Set a roomtone WAV file — roomtone operations section appears
    12. Select waveform region, click "Delete" — region removed, waveform reloads
    13. Use flippers — navigates only to chapters with CRX entries
    14. Commit all CRX entries for a chapter — completion badge, auto-advance
    15. Check corrected.wav bit depth matches source (ffprobe verification)
  </action>
  <verify>User confirms all 15 verification steps pass by typing "approved".</verify>
  <done>Full pickup substitution workflow verified end-to-end. All features functional, audio quality preserved, UI responsive.</done>
</task>

</tasks>

<verification>
Human visual and functional verification of the complete pickup substitution workflow.
</verification>

<success_criteria>
- All 15 verification steps pass
- No crashes or unhandled exceptions during the workflow
- Audio quality preserved (format-matching output)
- UI is responsive during processing and navigation
</success_criteria>

<output>
After completion, create `.planning/phases/13-pickup-substitution/13-08-SUMMARY.md`
</output>
