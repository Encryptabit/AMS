---
phase: 12.1-mfa-refinement-for-pickup-timings
plan: 01
type: execute
wave: 1
depends_on: []
note_cross_phase_dep: "Phase 12.1 depends on Phase 12 (PickupMatchingService must exist). This is enforced by roadmap execution order, not intra-phase depends_on."
files_modified:
  - host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs
  - host/Ams.Workstation.Server/Services/PickupMatchingService.cs
  - host/Ams.Workstation.Server/Program.cs
  - host/Ams.Core/Ams.Core.csproj
autonomous: true
requirements:
  - MFA-REFINE

must_haves:
  truths:
    - "Pickup matches returned by MatchPickupAsync have MFA-refined PickupStartSec/PickupEndSec timings instead of raw ASR token boundaries"
    - "MFA alignment runs on a temp corpus created from the pickup WAV and the matched sentences' BookText"
    - "TextGrid word intervals are mapped back to matched sentences using Needleman-Wunsch alignment (same strategy as MfaTimingMerger)"
    - "If MFA alignment fails (OOV, corpus issues), the original ASR-based timings are preserved as fallback"
    - "MFA results are cached on disk alongside ASR cache so re-import is instant"
  artifacts:
    - path: "host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs"
      provides: "MFA-based timing refinement for pickup matches"
      min_lines: 120
    - path: "host/Ams.Workstation.Server/Services/PickupMatchingService.cs"
      provides: "Updated pickup matching with MFA refinement post-processing"
      contains: "PickupMfaRefinementService"
  key_links:
    - from: "host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs"
      to: "MfaProcessSupervisor + MfaService"
      via: "MfaService.AlignAsync with MfaChapterContext"
      pattern: "AlignAsync"
    - from: "host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs"
      to: "TextGridParser"
      via: "ParseWordIntervals to get word-level timing intervals"
      pattern: "TextGridParser\\.ParseWordIntervals"
    - from: "host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs"
      to: "PronunciationHelper"
      via: "ExtractPronunciationParts for .lab file normalization"
      pattern: "PronunciationHelper\\.ExtractPronunciationParts|PrepareLabLine"
    - from: "host/Ams.Workstation.Server/Services/PickupMatchingService.cs"
      to: "host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs"
      via: "Post-ASR refinement call in MatchPickupAsync"
      pattern: "RefineWithMfaAsync"
---

<objective>
Run pickup audio through MFA forced alignment to replace rough ASR token boundaries with phoneme-accurate sentence timings.

Purpose: ASR (Whisper) gives word-level timestamps that are approximate. MFA forced alignment uses the acoustic model with known text to produce sub-phoneme-accurate boundaries. For pickup replacements, precise sentence boundaries mean cleaner crossfade splices with no clipped words or excess silence.

Output: PickupMfaRefinementService that runs MFA on matched pickup audio, and integration into PickupMatchingService so all returned PickupMatch records have MFA-refined timings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@host/Ams.Workstation.Server/Services/PickupMatchingService.cs
@host/Ams.Workstation.Server/Services/PolishService.cs
@host/Ams.Workstation.Server/Models/PolishModels.cs
@host/Ams.Core/Application/Mfa/MfaService.cs
@host/Ams.Core/Application/Mfa/MfaWorkflow.cs
@host/Ams.Core/Application/Mfa/Models/MfaChapterContext.cs
@host/Ams.Core/Application/Processes/MfaProcessSupervisor.cs
@host/Ams.Core/Processors/Alignment/Mfa/TextGridParser.cs
@host/Ams.Core/Processors/Alignment/Mfa/MfaTimingMerger.cs
@host/Ams.Core/Runtime/Book/PronunciationHelper.cs
@host/Ams.Core/Artifacts/Hydrate/HydratedTranscript.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PickupMfaRefinementService</name>
  <files>host/Ams.Workstation.Server/Services/PickupMfaRefinementService.cs</files>
  <action>
Create `PickupMfaRefinementService` in the Workstation.Server Services namespace. This service takes a list of ASR-matched `PickupMatch` records, the pickup WAV path, and the corresponding `HydratedSentence` list, then runs MFA forced alignment to produce phoneme-accurate sentence boundaries.

**Constructor dependencies:** `BlazorWorkspace` (for working directory).

**Public method:** `RefineWithMfaAsync(string pickupFilePath, IReadOnlyList<PickupMatch> asrMatches, IReadOnlyList<HydratedSentence> flaggedSentences, CancellationToken ct) -> Task<IReadOnlyList<PickupMatch>>`

**Implementation flow:**

1. **Check MFA cache** -- Cache key includes BOTH the pickup file identity AND the matched sentence context: SHA256 of `fullpath|size|lastwrite|sentenceId1:sentenceId2:...|normalizedBookText1|normalizedBookText2|...` (ordered by PickupStartSec). Store as `{hash}.mfa.json` at `{workDir}/.polish/pickups/`. This ensures same audio with different sentence matches doesn't return stale timings. If cache hit, deserialize and return directly.

2. **Build temp corpus directory** at `{workDir}/.polish/pickups/mfa/{hash}/`:
   - Copy pickup WAV as `pickup.wav` (use `File.Copy`)
   - Write `pickup.lab` containing the concatenated BookText of ALL matched sentences (in match order by PickupStartSec), normalized through `PronunciationHelper.ExtractPronunciationParts()` and joined with spaces. Use the same PrepareLabLine pattern from MfaWorkflow: for each sentence's BookText, call `PronunciationHelper.ExtractPronunciationParts(bookText)`, join parts with space, then join all sentence lines with newline into the .lab file content. Actually -- MFA .lab files are a single block of text (not per-line sentences), so join ALL sentence parts into one continuous line separated by spaces.

3. **Ensure MFA supervisor is warm** -- call `MfaProcessSupervisor.EnsureReadyAsync(ct)`.

4. **Build MfaChapterContext:**
   ```
   CorpusDirectory = corpusDir (the temp dir with pickup.wav + pickup.lab)
   OutputDirectory = Path.Combine(corpusDir, "output")
   WorkingDirectory = corpusDir
   DictionaryModel = MfaService.DefaultDictionaryModel
   AcousticModel = MfaService.DefaultAcousticModel
   Beam = 80
   RetryBeam = 200
   SingleSpeaker = true
   CleanOutput = true
   ```

5. **Run MFA validate + G2P + align** -- Follow the same OOV handling pattern as MfaWorkflow.RunChapterAsync but simplified (no corpus fallback needed since pickup text is known-good from ASR match):
   - Create `MfaService(useDedicatedProcess: false)` directly (same as MfaWorkflow does)
   - Run validate (optional, skip if no OOV file found)
   - If OOVs found, run G2P + AddWords to create custom dictionary
   - Run `service.AlignAsync(context, ct)` -- this produces `{outputDir}/pickup.TextGrid` (or `{outputDir}/alignment/mfa/pickup.TextGrid` depending on MFA version)

6. **Parse TextGrid** -- Find the TextGrid file (check both candidate paths like MfaWorkflow does), call `TextGridParser.ParseWordIntervals(textGridPath)` to get `IReadOnlyList<TextGridInterval>`.

7. **Map word intervals to sentences using Needleman-Wunsch alignment** (REQUIRED approach — handles word count mismatches from contractions, hyphen splits, OOV tokens robustly):
   - For each matched sentence, normalize BookText words using `PronunciationHelper.ExtractPronunciationParts()`.
   - Build a flat list of all expected words across all matched sentences (ordered by PickupStartSec), tracking which sentence each word belongs to.
   - Assign synthetic "bookIdx" values: sentence 0 words get indices 0..N-1, sentence 1 words get indices N..N+M-1, etc.
   - Track sentenceRanges: `{sentenceId, startIdx, endIdx}` mapping each sentence to its synthetic index range.
   - Create `TextGridWord[]` from parsed intervals (filter empty/silence text entries like `""`, `"sp"`, `"sil"`).
   - Create `SentenceTarget[]` from sentenceRanges where SetTiming captures `(start, end, duration)`.
   - Create a `getBookToken(idx)` function from the flat expected word list.
   - Call `MfaTimingMerger.MergeAndApply(textGridWords, getBookToken, 0, totalWords-1, wordTargets, sentenceTargets)`.
   - Read back each sentence's MFA timing from the SentenceTarget callbacks.

8. **Build refined PickupMatch list** -- For each original ASR match, if MFA produced a timing for that sentence, create a new PickupMatch with the MFA timings. If MFA failed for a specific sentence, keep the original ASR timing. Preserve the original Confidence and RecognizedText.

9. **Cache results** -- Serialize the refined match list to `{hash}.mfa.json`.

10. **Cleanup** -- Delete the temp corpus directory (try-catch, don't fail if cleanup fails).

**Error handling:** Wrap the entire MFA flow in try-catch. If MFA fails at any stage (supervisor not available, alignment fails, TextGrid missing), log a warning and return the original ASR matches unchanged. This ensures the pickup import workflow never breaks even if MFA is unavailable.

**Important implementation details:**
- `MfaProcessSupervisor.EnsureReadyAsync` and `MfaService` are `internal` -- the Workstation.Server project already references Ams.Core. Check if InternalsVisibleTo is set. If not, use the public surface: `MfaProcessSupervisor.TriggerBackgroundWarmup()` is internal too. Actually, looking at MfaWorkflow, it creates `new MfaService(useDedicatedProcess: false)` which is `internal` constructor. Since MfaWorkflow is itself `internal` to Ams.Core, the workstation server can't call it directly. **Resolution:** Add `[assembly: InternalsVisibleTo("Ams.Workstation.Server")]` to Ams.Core if not already present, OR create a thin public API on MfaService. Check for existing InternalsVisibleTo first. If it exists, use the internal APIs directly. If not, add the attribute to a suitable file in Ams.Core (e.g., create `Properties/AssemblyAttributes.cs` or add to an existing one).
- Actually, MfaWorkflow.PrepareLabLine is `private static`. We don't need it -- just use `PronunciationHelper.ExtractPronunciationParts()` directly (which is what PrepareLabLine calls). PronunciationHelper is public.
- MfaTimingMerger.MergeAndApply is public. TextGridParser.ParseWordIntervals is public. These can be called directly.
  </action>
  <verify>
Build succeeds: `dotnet build "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj" -c Debug`

Verify the service file exists and contains:
- RefineWithMfaAsync method signature
- MfaService.AlignAsync call
- TextGridParser.ParseWordIntervals call
- MfaTimingMerger.MergeAndApply call with synthetic bookIdx values
- MFA cache read/write
- try-catch fallback returning original ASR matches on failure
  </verify>
  <done>
PickupMfaRefinementService exists with RefineWithMfaAsync that: (1) creates temp MFA corpus from pickup WAV + matched sentence BookText, (2) runs MFA forced alignment via MfaService, (3) parses TextGrid and maps word intervals back to sentences using Needleman-Wunsch alignment, (4) returns refined PickupMatch records with MFA-accurate timings, (5) caches results for instant re-import, (6) falls back to ASR timings gracefully on any MFA failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate MFA refinement into PickupMatchingService</name>
  <files>host/Ams.Workstation.Server/Services/PickupMatchingService.cs, host/Ams.Workstation.Server/Program.cs, host/Ams.Core/Ams.Core.csproj</files>
  <action>
Wire PickupMfaRefinementService into PickupMatchingService so MFA refinement runs automatically after ASR matching.

1. **Add dependency** -- Add a `PickupMfaRefinementService` field to `PickupMatchingService`. Accept it via constructor injection. Update the constructor signature: `PickupMatchingService(BlazorWorkspace workspace, PickupMfaRefinementService mfaRefinement)`.

2. **Call MFA refinement in MatchPickupAsync** -- After the ASR matching loop produces the `matches` list (line ~117 in current code, right before `return matches.OrderBy(...)`), add:
   ```csharp
   // Refine ASR timings with MFA forced alignment
   if (matches.Count > 0)
   {
       matches = (await _mfaRefinement.RefineWithMfaAsync(
           pickupFilePath, matches, flaggedSentences, ct)
           .ConfigureAwait(false)).ToList();
   }
   ```
   Keep the existing `return matches.OrderBy(m => m.PickupStartSec).ToList();` after this.

3. **Register in DI** -- In `Program.cs`, add `PickupMfaRefinementService` as a transient/scoped service alongside PickupMatchingService. Also ensure MfaProcessSupervisor warmup is triggered at startup if not already (check if `MfaProcessSupervisor.TriggerBackgroundWarmup()` is called anywhere in the workstation startup path -- if not, add it so the MFA conda environment is warm by the time pickups are imported).

4. **Ensure InternalsVisibleTo** -- If `host/Ams.Core/Ams.Core.csproj` does not already expose internals to `Ams.Workstation.Server`, add `<InternalsVisibleTo Include="Ams.Workstation.Server" />` in an `<ItemGroup>` so that `MfaService` (internal) can be instantiated from the workstation project. Check existing `.csproj` first — it may already have this.

5. **No changes to MatchSinglePickupAsync** -- The single-pickup method is for individual files where the entire file IS the sentence. MFA refinement is most valuable for multi-sentence session pickups where sentence boundaries need to be discovered within a longer recording. For single pickups, the whole file duration IS the timing (PickupStartSec=0, PickupEndSec=duration), so MFA refinement of boundaries is not applicable.

**Verify DI registration compiles and the constructor chain is correct.** The service graph is: PolishService -> PickupMatchingService -> PickupMfaRefinementService.
  </action>
  <verify>
Build succeeds: `dotnet build "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj" -c Debug`

Verify in PickupMatchingService.cs:
- Constructor accepts PickupMfaRefinementService
- MatchPickupAsync calls RefineWithMfaAsync after ASR matching
- DI registration exists for PickupMfaRefinementService
  </verify>
  <done>
PickupMatchingService.MatchPickupAsync now: (1) runs ASR to identify which sentences are in the pickup, (2) calls PickupMfaRefinementService.RefineWithMfaAsync to get MFA-accurate boundaries, (3) returns matches with phoneme-precise PickupStartSec/PickupEndSec. DI wiring is complete. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build "C:\Projects\AMS\host\Ams.Workstation.Server\Ams.Workstation.Server.csproj" -c Debug` -- must succeed with no errors
2. PickupMfaRefinementService.cs exists and contains RefineWithMfaAsync method
3. PickupMatchingService.cs constructor accepts PickupMfaRefinementService and MatchPickupAsync calls RefineWithMfaAsync
4. MFA infrastructure chain is correct: PickupMfaRefinementService -> MfaService.AlignAsync -> MfaProcessSupervisor -> TextGridParser -> MfaTimingMerger
5. Graceful fallback: if MFA fails, original ASR timings are returned (no import failure)
</verification>

<success_criteria>
- Build passes with zero errors
- PickupMfaRefinementService creates temp corpus, runs MFA, parses TextGrid, maps to sentences
- PickupMatchingService.MatchPickupAsync returns MFA-refined timings
- MFA results cached to disk for instant re-import
- Fallback to ASR timings on MFA failure (no hard failure)
- DI registration complete
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-mfa-refinement-for-pickup-timings/12.1-01-SUMMARY.md`
</output>
