---
phase: 05-immediate-cleanup
plan: 01
type: execute
---

<objective>
Delete all dead code files identified in the v1.0 audit - broken projects, empty placeholders, and unused code.

Purpose: Remove ~493 lines of verified dead code to improve codebase health and reduce confusion.
Output: Clean codebase with 0 broken projects, 0 empty placeholders, 0 unused demo/legacy code.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recommendations/ACTION-LIST.md

**Prior decisions affecting this phase:**
- v1.0 audit verified all files as orphaned (zero callers confirmed)
- AUD-006: OverlayTest build broken, no value
- AUD-008: Wn*.cs files are empty placeholders (8 lines each)
- AUD-009: ManifestV2.cs superseded, zero callers
- AUD-010: Class1.cs is template leftover
- AUD-005: IAudioService/AudioService empty, never registered
- AUD-001: DspDemoRunner is demo code, zero callers
- AUD-002: SentenceTimelineBuilder superseded, zero callers

**Issue IDs addressed:** AUD-001, AUD-002, AUD-005, AUD-006, AUD-008, AUD-009, AUD-010
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete OverlayTest broken project</name>
  <files>analysis/OverlayTest/ (entire directory)</files>
  <action>
    1. Verify project is broken: Check if OverlayTest.csproj exists and has build errors
    2. Check solution file for reference: `Select-String "OverlayTest" *.sln`
    3. Remove from solution if present: Edit .sln to remove OverlayTest project reference
    4. Delete entire directory: `Remove-Item -Recurse -Force analysis/OverlayTest`

    WHY: Build is broken, no active use, 24 lines of worthless code.
  </action>
  <verify>
    - Directory no longer exists: `!(Test-Path analysis/OverlayTest)`
    - No solution reference: `!(Select-String "OverlayTest" *.sln)`
  </verify>
  <done>OverlayTest directory deleted, no solution references remain</done>
</task>

<task type="auto">
  <name>Task 2: Delete empty Whisper.NET placeholders</name>
  <files>
    host/Ams.Core/Services/Integrations/ASR/WhisperNet/WnModel.cs
    host/Ams.Core/Services/Integrations/ASR/WhisperNet/WnSession.cs
    host/Ams.Core/Services/Integrations/ASR/WhisperNet/WnTranscriber.cs
    host/Ams.Core/Services/Integrations/ASR/WhisperNet/WnUtils.cs
  </files>
  <action>
    1. Verify no callers for each file:
       - `Select-String -Recurse "WnModel" host/ -Include "*.cs" | Where {$_.Path -notmatch "WnModel.cs"}`
       - `Select-String -Recurse "WnSession" host/ -Include "*.cs" | Where {$_.Path -notmatch "WnSession.cs"}`
       - `Select-String -Recurse "WnTranscriber" host/ -Include "*.cs" | Where {$_.Path -notmatch "WnTranscriber.cs"}`
       - `Select-String -Recurse "WnUtils" host/ -Include "*.cs" | Where {$_.Path -notmatch "WnUtils.cs"}`
    2. Delete all 4 files using git rm
    3. Build to verify no breaks

    WHY: Empty placeholder classes (8 lines each), never implemented, zero callers.
  </action>
  <verify>
    - Files deleted: `!(Test-Path host/Ams.Core/Services/Integrations/ASR/WhisperNet/Wn*.cs)`
    - Build succeeds: `dotnet build host/Ams.Core --no-restore`
  </verify>
  <done>All 4 Wn*.cs files deleted, build succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Delete template artifacts and superseded files</name>
  <files>
    host/Ams.Web.Shared/Class1.cs
    host/Ams.Core/Pipeline/ManifestV2.cs
    host/Ams.Core/Services/Interfaces/IAudioService.cs
    host/Ams.Core/Services/AudioService.cs
  </files>
  <action>
    1. Verify no callers for each:
       - Class1.cs: Template leftover, check no imports
       - ManifestV2.cs: `Select-String -Recurse "ManifestV2" host/ -Include "*.cs"`
       - IAudioService: `Select-String -Recurse "IAudioService" host/ -Include "*.cs"`
       - AudioService: `Select-String -Recurse "AudioService" host/ -Include "*.cs" | Where {$_.Path -notmatch "AudioService.cs"}`
    2. Delete all 4 files using git rm
    3. Build each affected project

    WHY:
    - Class1.cs: 5-line template artifact
    - ManifestV2.cs: 19 lines, superseded format
    - IAudioService/AudioService: 18 lines total, empty implementation, never registered in DI
  </action>
  <verify>
    - Files deleted: Confirm each path no longer exists
    - Build succeeds: `dotnet build host/Ams.Core --no-restore && dotnet build host/Ams.Web.Shared --no-restore`
  </verify>
  <done>Class1.cs, ManifestV2.cs, IAudioService.cs, AudioService.cs all deleted, builds succeed</done>
</task>

<task type="auto">
  <name>Task 4: Delete demo and unused code</name>
  <files>
    host/Ams.Core/Audio/DspDemoRunner.cs
    host/Ams.Core/Audio/SentenceTimelineBuilder.cs
  </files>
  <action>
    1. Verify no callers:
       - `Select-String -Recurse "DspDemoRunner" host/ -Include "*.cs" | Where {$_.Path -notmatch "DspDemoRunner.cs"}`
       - `Select-String -Recurse "SentenceTimelineBuilder" host/ -Include "*.cs" | Where {$_.Path -notmatch "SentenceTimelineBuilder.cs"}`
    2. Delete both files using git rm
    3. Build to verify

    WHY:
    - DspDemoRunner.cs: 141 lines of demo code, never called in production
    - SentenceTimelineBuilder.cs: 182 lines, superseded by current pipeline
  </action>
  <verify>
    - Files deleted: Confirm paths no longer exist
    - Build succeeds: `dotnet build host/Ams.Core --no-restore`
  </verify>
  <done>DspDemoRunner.cs and SentenceTimelineBuilder.cs deleted, build succeeds</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build` succeeds for entire solution with 0 errors
- [ ] No references to deleted files remain in codebase
- [ ] Git status shows clean deletions ready to commit
</verification>

<success_criteria>
- All 10 dead code files deleted (~493 lines removed)
- Solution builds successfully
- No broken references
- Ready for commit
</success_criteria>

<output>
After completion, create `.planning/phases/05-immediate-cleanup/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Delete Dead Code Summary

**[One-liner describing what was deleted]**

## Accomplishments

- Deleted OverlayTest broken project (24 lines)
- Deleted 4 Wn*.cs placeholders (32 lines)
- Deleted Class1.cs template artifact (5 lines)
- Deleted ManifestV2.cs superseded file (19 lines)
- Deleted IAudioService/AudioService empty service (18 lines)
- Deleted DspDemoRunner.cs demo code (141 lines)
- Deleted SentenceTimelineBuilder.cs unused code (182 lines)

## Files Deleted

- `analysis/OverlayTest/` (entire directory)
- `host/Ams.Core/Services/Integrations/ASR/WhisperNet/Wn*.cs` (4 files)
- `host/Ams.Web.Shared/Class1.cs`
- `host/Ams.Core/Pipeline/ManifestV2.cs`
- `host/Ams.Core/Services/Interfaces/IAudioService.cs`
- `host/Ams.Core/Services/AudioService.cs`
- `host/Ams.Core/Audio/DspDemoRunner.cs`
- `host/Ams.Core/Audio/SentenceTimelineBuilder.cs`

## Issues Addressed

- AUD-001: DspDemoRunner.cs
- AUD-002: SentenceTimelineBuilder.cs
- AUD-005: IAudioService empty
- AUD-006: OverlayTest broken
- AUD-008: Wn*.cs placeholders
- AUD-009: ManifestV2.cs
- AUD-010: Class1.cs

## Decisions Made

None - all deletions pre-verified in v1.0 audit.

## Issues Encountered

[Document any unexpected callers found, or "None"]

## Next Step

Ready for 05-02-PLAN.md (Fix warning + interface)
</output>
