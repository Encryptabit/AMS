---
phase: 09-blazor-workstation
plan: 4
type: execute
---

<objective>
Wire up real Ams.Core data: stream audio from AudioBufferContext and populate SentenceList from HydratedTranscript.

Purpose: Replace mock data with actual chapter audio and sentence data from the pipeline artifacts.
Output: Functional chapter review with real audio streaming and sentence display.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-blazor-workstation/09-CONTEXT.md
@.planning/phases/09-blazor-workstation/09-03-SUMMARY.md

**Data access pattern (from BlazorWorkspace):**
```
workspace.CurrentChapterHandle.Context.Audio.Current.Buffer  → AudioBuffer
workspace.CurrentChapterHandle.Context.Documents.HydratedTranscript  → HydratedTranscript
```

**Key types:**
- `AudioBuffer.ToWavStream()` → MemoryStream of WAV data
- `HydratedTranscript.Sentences` → IReadOnlyList<HydratedSentence>
- `HydratedSentence.Timing` → TimingRange (StartSec, EndSec)
- `HydratedSentence.Status` → string (ok, warning, error)
- `HydratedSentence.Diff` → HydratedDiff with Ops for visualization

**Current files to modify:**
- `AudioController.cs` - currently serves from disk, needs to stream from AudioBuffer
- `ChapterDataService.cs` - currently returns mock data, needs to read HydratedTranscript
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AudioController to stream from AudioBufferContext</name>
  <files>host/Ams.Workstation.Server/Controllers/AudioController.cs</files>
  <action>
    Update AudioController to get audio from BlazorWorkspace:

    1. Inject BlazorWorkspace (scoped service)
    2. Update GetChapterAudio endpoint:
       ```csharp
       [HttpGet("chapter/{chapterName}")]
       public IActionResult GetChapterAudio(string chapterName)
       {
           if (_workspace.CurrentChapterHandle is null)
               return NotFound("No chapter loaded");

           var audioContext = _workspace.CurrentChapterHandle.Context.Audio.Current;
           var buffer = audioContext.Buffer;

           if (buffer is null)
               return NotFound("Audio buffer not available");

           var stream = buffer.ToWavStream();
           return File(stream, "audio/wav", enableRangeProcessing: true);
       }
       ```
    3. Keep the generic GetAudio endpoint for direct path access (backup)
    4. Remove sample.wav fallback from chapter endpoint
  </action>
  <verify>
    - `dotnet build host/Ams.Workstation.Server` succeeds
    - Endpoint compiles without errors
  </verify>
  <done>AudioController streams from AudioBufferContext</done>
</task>

<task type="auto">
  <name>Task 2: Update ChapterDataService to read HydratedTranscript</name>
  <files>host/Ams.Workstation.Server/Services/ChapterDataService.cs</files>
  <action>
    Update ChapterDataService to read from workspace:

    1. Inject BlazorWorkspace
    2. Update GetSentencesAsync to map HydratedSentence → SentenceViewModel:
       ```csharp
       public Task<List<SentenceViewModel>> GetSentencesAsync(string chapterName)
       {
           if (_workspace.CurrentChapterHandle is null)
               return Task.FromResult(new List<SentenceViewModel>());

           var hydrate = _workspace.CurrentChapterHandle.Context.Documents.HydratedTranscript;
           if (hydrate is null)
               return Task.FromResult(new List<SentenceViewModel>());

           var sentences = hydrate.Sentences.Select(s => new SentenceViewModel
           {
               Id = s.Id,
               Text = s.BookText,
               StartTime = s.Timing?.StartSec ?? 0,
               EndTime = s.Timing?.EndSec ?? 0,
               Status = s.Status ?? "ok",
               HasDiff = s.Diff is not null,
               DiffHtml = BuildDiffHtml(s.Diff)
           }).ToList();

           return Task.FromResult(sentences);
       }

       private static string? BuildDiffHtml(HydratedDiff? diff)
       {
           if (diff?.Ops is null) return null;
           // Build HTML from diff ops: equal=plain, delete=strikethrough, insert=underline
           var sb = new StringBuilder();
           foreach (var op in diff.Ops)
           {
               var tokens = string.Join(" ", op.Tokens);
               sb.Append(op.Operation switch
               {
                   "delete" => $"<span class=\"diff-delete\">{tokens}</span>",
                   "insert" => $"<span class=\"diff-insert\">{tokens}</span>",
                   _ => tokens
               });
               sb.Append(' ');
           }
           return sb.ToString().Trim();
       }
       ```
    3. Remove mock data entirely
  </action>
  <verify>
    - `dotnet build host/Ams.Workstation.Server` succeeds
    - Service compiles and maps HydratedSentence correctly
  </verify>
  <done>ChapterDataService reads from HydratedTranscript</done>
</task>

<task type="auto">
  <name>Task 3: Wire up ChapterReview with SentenceList and real data</name>
  <files>host/Ams.Workstation.Server/Components/Pages/Proof/ChapterReview.razor</files>
  <action>
    Complete the ChapterReview page wiring:

    1. Inject ChapterDataService
    2. Load sentences on chapter change
    3. Pass data to SentenceList component:
       ```razor
       <SentenceList
           Sentences="@_sentences"
           CurrentTime="@_currentTime"
           OnSentenceSelected="HandleSentenceSelected" />
       ```
    4. Handle sentence selection → seek waveform
    5. Pass CurrentTime from waveform → SentenceList for time-sync

    Note: Audio URL already points to chapter endpoint, just needs AudioController to return real data.
  </action>
  <verify>
    - `dotnet build host/Ams.Workstation.Server` succeeds
    - ChapterReview loads and displays sentences
    - Time sync between waveform and sentence list works
  </verify>
  <done>ChapterReview wired to SentenceList with real data flow</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Real data integration: audio streaming from AudioBuffer, sentences from HydratedTranscript</what-built>
  <how-to-verify>
    1. Set working directory to a folder with processed chapter data:
       - book-index.json
       - {chapter}/ folder with .hydrate.json and source audio
    2. Select a chapter from dropdown
    3. Navigate to /proof/{chapter}
    4. Verify: Waveform loads from actual chapter audio (not sample.wav)
    5. Verify: Sentence list shows actual sentences from hydrate.json
    6. Verify: Sentence timing matches audio playback position
    7. Check console for any errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build host/Ams.Workstation.Server` succeeds
- [ ] AudioController streams from AudioBuffer.ToWavStream()
- [ ] ChapterDataService maps HydratedTranscript.Sentences
- [ ] ChapterReview wired to SentenceList with data + callbacks
- [ ] Waveform loads real chapter audio
- [ ] Sentence list displays real sentences from hydrate.json
- [ ] Time sync works (playing audio updates active sentence)
- [ ] Clicking sentence seeks waveform
- [ ] No console errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verified real data flowing through UI
- Ready for keyboard navigation and layout refinement
</success_criteria>

<output>
After completion, create `.planning/phases/09-blazor-workstation/09-04-SUMMARY.md`
</output>
