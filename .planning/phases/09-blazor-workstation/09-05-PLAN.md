---
phase: 09-blazor-workstation
plan: 05
type: execute
---

<objective>
Consolidate chapter discovery logic into Ams.Core so hosts only provide a directory path.

Purpose: Remove duplicate WAV scanning, section matching, and manual JSON loading from BlazorWorkspace. The CLI's ReplState pattern is battle-tested - extract it to Core for reuse.
Output: ChapterDiscoveryService in Ams.Core, refactored BlazorWorkspace using the service and FileArtifactResolver.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior decisions affecting this phase:**
- Workspace lifetime is Singleton (API/Blazor state sharing)
- Chapter resolution uses WAV stem via SectionLocator
- FileArtifactResolver is the canonical way to load/save artifacts

**Reference implementations (battle-tested patterns to extract):**
@host/Ams.Cli/Repl/ReplContext.cs - ReplState.RefreshChapters(), ChapterFileComparer
@host/Ams.Core/Runtime/Artifacts/FileArtifactResolver.cs - LoadBookIndex(), IArtifactResolver pattern
@host/Ams.Core/Processors/Alignment/Anchors/SectionLocator.cs - ResolveSectionByTitle()

**Files to refactor:**
@host/Ams.Workstation.Server/Services/BlazorWorkspace.cs - Remove manual logic, use new service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChapterDiscoveryService in Ams.Core</name>
  <files>host/Ams.Core/Runtime/Chapter/ChapterDiscoveryService.cs</files>
  <action>
Create a new service that consolidates chapter discovery logic:

1. Create `ChapterDiscoveryService` class in `Ams.Core.Runtime.Chapter` namespace
2. Define `ChapterInfo` record: `{ string Stem, string DisplayTitle, string WavPath }`
3. Implement `DiscoverChapters(string rootPath, IArtifactResolver resolver)` method:
   - Use resolver to get book-index path, load BookIndex via resolver pattern
   - Scan for WAV files: `Directory.EnumerateFiles(rootPath, "*.wav", SearchOption.TopDirectoryOnly)`
   - For each WAV, use `SectionLocator.ResolveSectionByTitle()` to map stem to section
   - Build ChapterInfo list with stem, display title (section.Title), and full WAV path
   - Sort using extracted `ChapterFileComparer` logic (numeric-first sorting from ReplState)
4. Return `IReadOnlyList<ChapterInfo>` sorted by chapter order

Extract the `ChapterFileComparer` sorting logic from ReplState - it handles numeric prefixes correctly (e.g., "01_Chapter" sorts before "02_Chapter").

Do NOT modify ReplState yet - just extract the pattern. ReplState can be updated in a future plan if desired.
  </action>
  <verify>
Build succeeds: `dotnet build host/Ams.Core/Ams.Core.csproj`
New file exists at expected path
  </verify>
  <done>
ChapterDiscoveryService exists with DiscoverChapters method that:
- Takes rootPath and resolver
- Returns sorted list of ChapterInfo (stem, title, path)
- Uses SectionLocator for section matching
- Uses numeric-aware sorting
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor BlazorWorkspace to use ChapterDiscoveryService</name>
  <files>host/Ams.Workstation.Server/Services/BlazorWorkspace.cs, host/Ams.Workstation.Server/Program.cs</files>
  <action>
Refactor BlazorWorkspace to use the new ChapterDiscoveryService:

1. Add ChapterDiscoveryService as a dependency (inject or use static/singleton pattern matching FileArtifactResolver)
2. Replace `LoadChaptersFromIndex()` method:
   - Remove manual `File.ReadAllText` + `JsonSerializer.Deserialize<BookIndex>`
   - Remove manual `Directory.GetFiles(..., "*.wav")`
   - Remove manual `SectionLocator.ResolveSectionByTitle()` loop
   - Replace with single call to `ChapterDiscoveryService.DiscoverChapters()`
   - Store results in `_discoveredChapters` field (List<ChapterInfo>)
   - Populate `AvailableChapters` from ChapterInfo.DisplayTitle
   - Populate `_stemByTitle` dictionary from ChapterInfo
3. Update `HasBookIndex` property to use resolver pattern if possible (or keep simple File.Exists - acceptable for property)
4. In `SelectChapter()`:
   - Look up ChapterInfo by display title
   - Use ChapterInfo.Stem and ChapterInfo.WavPath instead of manual Path.Combine
   - Let ChapterManager.CreateContext() handle remaining path resolution

Remove these manual operations from BlazorWorkspace:
- Manual JSON deserialization of book-index.json
- Manual WAV file scanning
- Manual section matching loop
- Manual path construction for audio files (use ChapterInfo.WavPath)

Keep the _stemByTitle dictionary but populate it from ChapterInfo results.
  </action>
  <verify>
Build succeeds: `dotnet build host/Ams.Workstation.Server/Ams.Workstation.Server.csproj`
No manual JsonSerializer.Deserialize<BookIndex> in BlazorWorkspace
No Directory.GetFiles or EnumerateFiles for *.wav in BlazorWorkspace
  </verify>
  <done>
BlazorWorkspace uses ChapterDiscoveryService for chapter discovery
Manual JSON loading removed
Manual WAV scanning removed
Manual section matching removed
_stemByTitle populated from ChapterInfo results
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration verification</name>
  <files>N/A - verification only</files>
  <action>
Verify the refactored workstation works correctly:

1. Build entire solution: `dotnet build`
2. Verify no new warnings related to the changed files
3. Check that ChapterDiscoveryService is properly accessible from Workstation project (reference exists)
4. Grep to confirm removal of anti-patterns:
   - No `JsonSerializer.Deserialize<BookIndex>` in BlazorWorkspace.cs
   - No `Directory.GetFiles` or `Directory.EnumerateFiles` for "*.wav" in BlazorWorkspace.cs
   - SectionLocator calls should only be in ChapterDiscoveryService (consolidation complete)
  </action>
  <verify>
`dotnet build` succeeds with no errors
Grep confirms anti-patterns removed from BlazorWorkspace
  </verify>
  <done>
Solution builds cleanly
BlazorWorkspace no longer contains manual discovery logic
ChapterDiscoveryService is the single source of chapter discovery
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build` succeeds for entire solution
- [ ] ChapterDiscoveryService exists in Ams.Core.Runtime.Chapter
- [ ] BlazorWorkspace uses ChapterDiscoveryService (no manual WAV/JSON logic)
- [ ] No regression in chapter discovery functionality
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Chapter discovery logic consolidated into Ams.Core
- BlazorWorkspace is thinner (delegates to Core service)
- Hosts now only need to provide directory path
</success_criteria>

<output>
After completion, create `.planning/phases/09-blazor-workstation/09-05-SUMMARY.md`:

# Phase 9 Plan 5: Chapter Discovery Consolidation Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.cs` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Deviations from Plan

[If any deviations occurred, document them here with the rule applied]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

[Ready for next plan or phase complete]
</output>
