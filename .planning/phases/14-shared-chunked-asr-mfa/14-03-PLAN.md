---
phase: 14-shared-chunked-asr-mfa
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - host/Ams.Core/Services/AsrService.cs
  - host/Ams.Core/Application/Commands/GenerateTranscriptCommand.cs
  - host/Ams.Core/Services/Interfaces/IAsrService.cs
autonomous: true
requirements: [CHUNK-ASR]

must_haves:
  truths:
    - "ASR uses persisted shared chunk plan when available"
    - "ASR chunking remains non-overlapping to avoid duplicate boundary token decode"
    - "ASR output contract and artifact shape remain unchanged"
  artifacts:
    - path: "host/Ams.Core/Services/AsrService.cs"
      provides: "ASR execution driven by chunk plan artifact"
      contains: "TranscribeAsync"
  key_links:
    - from: "host/Ams.Core/Services/AsrService.cs"
      to: "host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs"
      via: "Read/write ChunkPlan through chapter documents"
      pattern: "ChunkPlan"
---

<objective>
Refactor ASR execution to consume the shared chunk-plan artifact rather than ad-hoc chunk discovery.

Purpose: Ensure ASR and MFA use identical chunk boundaries and avoid stage drift.
Output: ASR execution path that loads or creates chunk plan once, then transcribes per chunk with existing merge behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-chunked-asr-mfa/14-CONTEXT.md
@.planning/phases/14-shared-chunked-asr-mfa/14-01-PLAN.md
@.planning/phases/14-shared-chunked-asr-mfa/14-02-PLAN.md

@host/Ams.Core/Services/AsrService.cs
@host/Ams.Core/Processors/AsrProcessor.cs
@host/Ams.Core/Runtime/Chapter/ChapterContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use chunk-plan artifact in ASR flow</name>
  <files>
    host/Ams.Core/Services/AsrService.cs
  </files>
  <action>
1. In ASR transcription flow:
   - attempt to read `chapter.Documents.ChunkPlan`
   - if missing/stale, generate via ChunkPlanningService and persist through chapter documents

2. Replace direct `SilenceChunker.FindChunkBoundaries` calls with plan-driven boundaries.

3. Keep existing behavior for single-chunk optimization and chunk response merge into one AsrResponse.

4. Ensure loaded `AudioBuffer` slicing uses chunk sample boundaries from plan entries.
  </action>
  <verify>
`dotnet build "C:\Projects\AMS\host\Ams.Core\Ams.Core.csproj" -c Debug`
  </verify>
  <done>
ASR stage reads/writes chunk plan artifact and uses it as the sole source of chunk boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve non-overlap semantics and output stability</name>
  <files>
    host/Ams.Core/Services/AsrService.cs
    host/Ams.Core/Application/Commands/GenerateTranscriptCommand.cs
  </files>
  <action>
1. Keep non-overlapping ASR chunk decode semantics by default.

2. Add guardrails in merge logic:
   - monotonic timestamps
   - deterministic token/segment ordering
   - no duplicated chunk offset application

3. Confirm no downstream artifact shape changes:
   - `AsrResponse`
   - `*.asr.json`
   - corpus text generation behavior

4. Add targeted tests (or extend existing ASR/chunk tests) for:
   - plan-driven multi-chunk transcribe
   - non-overlap duplicate prevention at boundaries.
  </action>
  <verify>
1. `dotnet test "C:\Projects\AMS\host\Ams.Tests\Ams.Tests.csproj" -c Debug --filter Asr`
2. Compare output token count/timestamp continuity before vs after for representative chapter.
  </verify>
  <done>
ASR remains behaviorally stable while switching to shared chunk plan input.
  </done>
</task>

</tasks>

<verification>
- ASR no longer computes independent chunk boundaries
- Existing ASR artifact consumers continue to work without modification
</verification>

<success_criteria>
- Shared chunk plan is now authoritative for ASR chunk execution
- Boundary duplicate risk remains controlled via non-overlap semantics
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-chunked-asr-mfa/14-03-SUMMARY.md`
</output>
