---
phase: 14-shared-chunked-asr-mfa
plan: 05
type: execute
wave: 3
depends_on: ["14-04"]
files_modified:
  - host/Ams.Core/Application/Mfa/TextGridAggregationService.cs
  - host/Ams.Core/Application/Mfa/MfaWorkflow.cs
  - host/Ams.Core/Processors/Alignment/Mfa/TextGridParser.cs
autonomous: true
requirements: [CHUNK-TEXTGRID]

must_haves:
  truths:
    - "Per-chunk MFA outputs are aggregated back into canonical chapter-level TextGrid"
    - "Chunk offsets are applied exactly once during aggregation"
    - "MergeTimingsCommand contract remains unchanged (single chapter TextGrid input)"
  artifacts:
    - path: "host/Ams.Core/Application/Mfa/TextGridAggregationService.cs"
      provides: "Deterministic chunk TextGrid to chapter TextGrid aggregation"
      contains: "TextGridAggregationService"
    - path: "host/Ams.Core/Application/Mfa/MfaWorkflow.cs"
      provides: "Aggregation step invoked after chunk align completes"
      contains: "TextGrid"
  key_links:
    - from: "host/Ams.Core/Application/Mfa/TextGridAggregationService.cs"
      to: "host/Ams.Core/Application/Commands/MergeTimingsCommand.cs"
      via: "Canonical output path and parse expectations preserved"
      pattern: "ParseWordIntervals"
---

<objective>
Aggregate chunk-level MFA alignment outputs into one canonical chapter TextGrid artifact compatible with current merge flow.

Purpose: Keep downstream timing merge and artifact contracts stable while enabling chunked MFA alignment internally.
Output: TextGrid aggregation service + workflow integration preserving existing chapter TextGrid path.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-chunked-asr-mfa/14-CONTEXT.md
@.planning/phases/14-shared-chunked-asr-mfa/14-04-PLAN.md

@host/Ams.Core/Application/Mfa/MfaWorkflow.cs
@host/Ams.Core/Application/Commands/MergeTimingsCommand.cs
@host/Ams.Core/Processors/Alignment/Mfa/TextGridParser.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build chunk TextGrid aggregation service</name>
  <files>
    host/Ams.Core/Application/Mfa/TextGridAggregationService.cs
  </files>
  <action>
1. Implement service that:
   - reads per-chunk TextGrid outputs
   - applies chunk start offsets to interval timestamps
   - merges and orders intervals into one chapter timeline

2. Preserve tier naming conventions expected by existing parser (`words` tier at minimum).

3. Emit canonical chapter-level TextGrid at existing destination used by workflow/merge.

4. Handle chunk gaps/empty chunk outputs gracefully with explicit logging.
  </action>
  <verify>
`dotnet build "C:\Projects\AMS\host\Ams.Core\Ams.Core.csproj" -c Debug`
  </verify>
  <done>
TextGridAggregationService can generate a canonical chapter-level TextGrid from chunked MFA outputs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate aggregation into MFA workflow</name>
  <files>
    host/Ams.Core/Application/Mfa/MfaWorkflow.cs
    host/Ams.Core/Processors/Alignment/Mfa/TextGridParser.cs
  </files>
  <action>
1. Update `MfaWorkflow` to invoke aggregation after chunked align export.

2. Keep copy/export behavior for debug artifacts (`*.g2p.txt`, dictionary, analysis) intact.

3. Verify `MergeTimingsCommand` works unchanged against aggregated chapter TextGrid.

4. Add tests for offset correctness and interval ordering monotonicity.
  </action>
  <verify>
1. `dotnet test "C:\Projects\AMS\host\Ams.Tests\Ams.Tests.csproj" -c Debug --filter MfaTiming`
2. `pipeline run` with MFA enabled completes and merge stats remain sane.
  </verify>
  <done>
Chunked MFA internals are transparent to existing merge and artifact consumers.
  </done>
</task>

</tasks>

<verification>
- Chapter-level canonical TextGrid path remains stable
- MergeTimingsCommand requires no interface changes
</verification>

<success_criteria>
- Chunked MFA is operational without breaking downstream timing merge pipeline
- Aggregated intervals are offset-correct and time-ordered
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-chunked-asr-mfa/14-05-SUMMARY.md`
</output>
