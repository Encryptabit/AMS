---
phase: 14-shared-chunked-asr-mfa
plan: 07
type: execute
wave: 4
depends_on: ["14-03", "14-04", "14-05", "14-06"]
files_modified:
  - host/Ams.Cli/Commands/PipelineCommand.cs
  - host/Ams.Core/Application/Pipeline/PipelineConcurrencyControl.cs
  - host/Ams.Core/Services/PipelineService.cs
  - host/Ams.Core/Application/Mfa/MfaWorkspaceResolver.cs
  - .planning/phases/14-shared-chunked-asr-mfa/14-VERIFICATION.md
autonomous: true
requirements: [ROLLout-HARDENING]

must_haves:
  truths:
    - "Shared chunking rollout is guarded by flags and can be reverted quickly"
    - "ASR and MFA concurrency settings are tuned independently for GPU vs CPU constraints"
    - "MFA workspace and artifact hygiene remain stable under parallel chapter execution"
  artifacts:
    - path: "host/Ams.Cli/Commands/PipelineCommand.cs"
      provides: "Operational controls for chunked ASR/MFA rollout and tuning"
      contains: "max-asr|max-mfa"
    - path: ".planning/phases/14-shared-chunked-asr-mfa/14-VERIFICATION.md"
      provides: "Benchmark and parity checklist for rollout acceptance"
  key_links:
    - from: "host/Ams.Core/Services/PipelineService.cs"
      to: "host/Ams.Core/Application/Pipeline/PipelineConcurrencyControl.cs"
      via: "Stage semaphores and MFA workspace leasing"
      pattern: "AsrSemaphore|MfaSemaphore|RentMfaWorkspace"
---

<objective>
Operationalize shared chunking with safe rollout controls, tuned concurrency defaults, and verification artifacts.

Purpose: Move from implementation completion to production-safe adoption with measurable throughput/quality outcomes.
Output: Feature flags, tuned parallel behavior, and phase verification document.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-chunked-asr-mfa/14-CONTEXT.md
@.planning/phases/14-shared-chunked-asr-mfa/14-03-PLAN.md
@.planning/phases/14-shared-chunked-asr-mfa/14-04-PLAN.md
@.planning/phases/14-shared-chunked-asr-mfa/14-05-PLAN.md
@.planning/phases/14-shared-chunked-asr-mfa/14-06-PLAN.md

@host/Ams.Cli/Commands/PipelineCommand.cs
@host/Ams.Core/Application/Pipeline/PipelineConcurrencyControl.cs
@host/Ams.Core/Services/PipelineService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rollout controls and tuned defaults</name>
  <files>
    host/Ams.Cli/Commands/PipelineCommand.cs
    host/Ams.Core/Services/PipelineService.cs
    host/Ams.Core/Application/Pipeline/PipelineConcurrencyControl.cs
  </files>
  <action>
1. Add feature flags/options for:
   - shared chunk plan enablement
   - chunked MFA corpus enablement
   - MFA profile default

2. Set practical default guidance:
   - ASR concurrency constrained (typically 1 for large Whisper GPU model)
   - MFA concurrency scaled by CPU and available chunks

3. Ensure fallback path can disable chunked MFA quickly without code rollback.

4. Preserve existing pipeline stage controls and backward compatibility.
  </action>
  <verify>
`dotnet build "C:\Projects\AMS\host\Ams.sln" -c Release`
  </verify>
  <done>
Operators can enable/disable shared chunking behavior and tune concurrency/profile settings safely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create verification and benchmark checklist</name>
  <files>
    .planning/phases/14-shared-chunked-asr-mfa/14-VERIFICATION.md
    host/Ams.Core/Application/Mfa/MfaWorkspaceResolver.cs
  </files>
  <action>
1. Add a phase verification doc containing:
   - baseline vs new wall-clock timings
   - ASR/MFA per-stage timing deltas
   - quality parity checks (merge coverage, sentence update counts)
   - failure/retry incidence metrics

2. Include explicit run matrix:
   - single chapter
   - all chapters (`mode all`) with varying `max-mfa`
   - profile comparison (`fast`, `balanced`, `strict`)

3. Verify MFA workspace isolation/cleanup under parallel load and document expected behavior.
  </action>
  <verify>
1. Execute benchmark matrix and record results in `14-VERIFICATION.md`
2. Confirm no workspace corruption or lingering file-handle failures in multi-chapter runs.
  </verify>
  <done>
Phase has clear go/no-go criteria for promoting shared chunking as default behavior.
  </done>
</task>

</tasks>

<verification>
- Shared chunking can be rolled out incrementally and reverted by configuration
- Throughput gains and quality parity are both evidenced in verification artifacts
</verification>

<success_criteria>
- Production-safe operational controls are in place
- Benchmark evidence supports default adoption decision
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-chunked-asr-mfa/14-07-SUMMARY.md`
</output>
