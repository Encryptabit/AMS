---
phase: 14-shared-chunked-asr-mfa
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - host/Ams.Core/Services/Alignment/ChunkPlanningService.cs
  - host/Ams.Core/Audio/SilenceChunker.cs
  - host/Ams.Core/Application/Pipeline/PipelineRunOptions.cs
autonomous: true
requirements: [CHUNK-PLANNER]

must_haves:
  truths:
    - "Chunk plan generation is deterministic for identical audio and policy inputs"
    - "Chunk planning uses AudioBufferContext-loaded audio and existing SilenceChunker primitives"
    - "Chunk policy is explicit and serializable for reproducibility"
  artifacts:
    - path: "host/Ams.Core/Services/Alignment/ChunkPlanningService.cs"
      provides: "Single source of chunk-plan generation for ASR and MFA"
      contains: "ChunkPlanningService"
  key_links:
    - from: "host/Ams.Core/Services/Alignment/ChunkPlanningService.cs"
      to: "host/Ams.Core/Audio/SilenceChunker.cs"
      via: "Chunk boundaries generated from silence analysis"
      pattern: "SilenceChunker.FindChunkBoundaries"
---

<objective>
Add a dedicated chunk planning service that computes a reusable chapter chunk plan from chapter audio.

Purpose: Avoid recomputing divergent chunk boundaries across stages and make chunking policy traceable in artifacts.
Output: ChunkPlanningService with deterministic plan generation and explicit policy controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-chunked-asr-mfa/14-CONTEXT.md
@.planning/phases/14-shared-chunked-asr-mfa/14-01-PLAN.md

@host/Ams.Core/Audio/SilenceChunker.cs
@host/Ams.Core/Runtime/Audio/AudioBufferManager.cs
@host/Ams.Core/Runtime/Audio/AudioBufferContext.cs
@host/Ams.Core/Runtime/Chapter/ChapterContext.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ChunkPlanningService</name>
  <files>
    host/Ams.Core/Services/Alignment/ChunkPlanningService.cs
  </files>
  <action>
Create a new service that:
1. Accepts chapter context + optional chunk policy
2. Loads chapter raw audio through existing chapter audio runtime APIs (`AudioBufferManager`/`AudioBufferContext`)
3. Invokes SilenceChunker with policy parameters
4. Converts sample boundaries to `ChunkPlanDocument` entries
5. Emits stable chunk ids in source order

Define `ChunkPlanningPolicy` with explicit knobs:
- silence threshold (dB)
- minimum silence duration
- minimum chunk duration

Ensure generated plan metadata stores policy values and source audio identity to support invalidation decisions.
  </action>
  <verify>
`dotnet build "C:\Projects\AMS\host\Ams.Core\Ams.Core.csproj" -c Debug`
  </verify>
  <done>
ChunkPlanningService can compute a valid ChunkPlanDocument for a chapter using existing audio runtime abstractions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Determinism + policy plumbing</name>
  <files>
    host/Ams.Core/Services/Alignment/ChunkPlanningService.cs
    host/Ams.Core/Application/Pipeline/PipelineRunOptions.cs
  </files>
  <action>
1. Add deterministic ordering guarantees:
   - chunk ids reflect sorted boundary order
   - floating-point conversion is consistent

2. Add policy option plumbing in pipeline options so future stages can pass explicit chunk policy (with sensible defaults).

3. Document expected invalidation rule:
   - regenerate when source audio fingerprint or chunk policy differs from stored plan.

4. Add unit tests for deterministic generation with stable fixture audio where practical.
  </action>
  <verify>
1. `dotnet test "C:\Projects\AMS\host\Ams.Tests\Ams.Tests.csproj" -c Debug --filter Chunk`
2. Confirm byte-identical serialized chunk plan across repeated runs on same input.
  </verify>
  <done>
Chunk planner output is deterministic and policy-driven, ready to be consumed by ASR and MFA stages.
  </done>
</task>

</tasks>

<verification>
- Same audio + same policy yields stable chunk plan document
- Chunk policy is explicit rather than implicit in stage-specific code
</verification>

<success_criteria>
- Shared chunk planning source exists and is usable by both ASR and MFA
- No stage computes its own independent boundary logic anymore once integrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-chunked-asr-mfa/14-02-SUMMARY.md`
</output>
