---
phase: 14-shared-chunked-asr-mfa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - host/Ams.Core/Artifacts/Alignment/ChunkPlanDocument.cs
  - host/Ams.Core/Runtime/Artifacts/IArtifactResolver.cs
  - host/Ams.Core/Runtime/Artifacts/FileArtifactResolver.cs
  - host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs
autonomous: true
requirements: [CHUNK-ARTIFACTS]

must_haves:
  truths:
    - "Chunk plan is a first-class chapter artifact managed through ChapterDocuments DocumentSlot<T>"
    - "Chunk artifact persistence is routed through IArtifactResolver and FileArtifactResolver"
    - "Chunk artifact filename follows existing chapter artifact naming conventions"
  artifacts:
    - path: "host/Ams.Core/Artifacts/Alignment/ChunkPlanDocument.cs"
      provides: "Canonical document model for chapter chunk planning"
      contains: "ChunkPlanDocument"
    - path: "host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs"
      provides: "ChunkPlan slot integrated with existing chapter document lifecycle"
      contains: "DocumentSlot<ChunkPlanDocument>"
  key_links:
    - from: "host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs"
      to: "host/Ams.Core/Runtime/Artifacts/IArtifactResolver.cs"
      via: "Load/Save/GetBackingFile calls for chunk artifact"
      pattern: "LoadChunkPlan|SaveChunkPlan|GetChunkPlanFile"
---

<objective>
Introduce a canonical chunk-plan artifact that follows the same architecture as transcript/hydrate/anchors/asr artifacts.

Purpose: Shared chunking across ASR and MFA must be represented as durable chapter state using the existing runtime abstractions (DocumentSlot, artifact resolver, chapter file conventions).
Output: New chunk document model + resolver integration + chapter document slot wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-shared-chunked-asr-mfa/14-CONTEXT.md

@host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs
@host/Ams.Core/Runtime/Artifacts/IArtifactResolver.cs
@host/Ams.Core/Runtime/Artifacts/FileArtifactResolver.cs
@host/Ams.Core/Runtime/Common/DocumentSlot.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define chunk plan artifact model</name>
  <files>
    host/Ams.Core/Artifacts/Alignment/ChunkPlanDocument.cs
  </files>
  <action>
Create a new canonical artifact model under `Ams.Core.Artifacts.Alignment`.

Minimum schema:
- `ChunkPlanDocument`:
  - `Version`
  - `CreatedAtUtc`
  - `SourceAudioPath`
  - `SourceAudioFingerprint` (or equivalent deterministic identity)
  - `Policy` metadata (silence threshold, min silence, min chunk duration)
  - `Chunks: IReadOnlyList<ChunkPlanEntry>`
- `ChunkPlanEntry`:
  - `ChunkId` (stable ordering id)
  - `StartSample`
  - `LengthSamples`
  - `StartSec`
  - `EndSec`

Use JSON shape conventions consistent with existing artifacts (camelCase serialization via resolver options).
Add XML docs clarifying this document is shared by ASR and MFA execution stages.
  </action>
  <verify>
`dotnet build "C:\Projects\AMS\host\Ams.Core\Ams.Core.csproj" -c Debug`
  </verify>
  <done>
ChunkPlanDocument and ChunkPlanEntry compile and are ready for resolver/document-slot wiring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire chunk artifact through resolver + chapter documents</name>
  <files>
    host/Ams.Core/Runtime/Artifacts/IArtifactResolver.cs
    host/Ams.Core/Runtime/Artifacts/FileArtifactResolver.cs
    host/Ams.Core/Runtime/Chapter/ChapterDocuments.cs
  </files>
  <action>
1. Extend `IArtifactResolver` with:
   - `LoadChunkPlan(ChapterContext context)`
   - `SaveChunkPlan(ChapterContext context, ChunkPlanDocument document)`
   - `GetChunkPlanFile(ChapterContext context)`

2. Implement resolver methods in `FileArtifactResolver` using chapter artifact naming:
   - `<chapterStem>.align.chunks.json`

3. Add `DocumentSlot<ChunkPlanDocument>` in `ChapterDocuments`:
   - property: `ChunkPlan`
   - save participation in `SaveChanges()`
   - backing file getter `GetChunkPlanFile()`

4. Preserve architecture consistency:
   - no ad-hoc file IO from stage services for chunk-plan persistence
   - all runtime reads/writes go through ChapterDocuments + resolver.
  </action>
  <verify>
1. `dotnet build "C:\Projects\AMS\host\Ams.sln" -c Debug`
2. Confirm chunk plan slot is included in `ChapterDocuments.IsDirty` and `SaveChanges()` flow.
  </verify>
  <done>
Chunk plan participates fully in chapter artifact lifecycle with DocumentSlot-managed persistence.
  </done>
</task>

</tasks>

<verification>
- New chunk-plan artifact is addressable through `chapter.Documents.ChunkPlan`
- Backing file path resolves through resolver as `<chapterStem>.align.chunks.json`
- No direct ad-hoc chunk-plan file writes remain outside resolver
</verification>

<success_criteria>
- Artifact architecture remains consistent with transcript/hydrate/anchors/asr
- Chunk plan is durable chapter state, not transient-only runtime data
</success_criteria>

<output>
After completion, create `.planning/phases/14-shared-chunked-asr-mfa/14-01-SUMMARY.md`
</output>
